<template><PublicPage eyebrow="Fale com a gente" title="Como podemos ajudar?" description="Envie sua mensagem e nossa equipe retornará em breve.">
  <div v-if="sent" class="max-w-2xl rounded-2xl border border-[color:var(--accent-soft-border)] bg-[color:var(--accent-soft-bg)] p-6" role="status" aria-live="polite"><h2 class="font-semibold">Mensagem enviada!</h2><p class="mt-2 text-[color:var(--text-muted)]">Obrigado pelo contato. Responderemos assim que possível.</p></div>
  <form v-else class="max-w-2xl space-y-5 rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-6 sm:p-8" @submit.prevent="submit(form)"><div class="grid gap-5 sm:grid-cols-2"><label for="contact-name">Nome<input id="contact-name" v-model="form.name" required autocomplete="name" class="field" /></label><label for="contact-email">Email<input id="contact-email" v-model="form.email" required type="email" autocomplete="email" class="field" /></label></div><label for="contact-subject">Assunto<input id="contact-subject" v-model="form.subject" required class="field" /></label><label for="contact-message">Mensagem<textarea id="contact-message" v-model="form.message" required rows="6" class="field"></textarea></label><p v-if="error" class="text-sm text-[color:var(--danger-text)]" role="alert" aria-live="assertive">{{ error }}</p><button type="submit" :disabled="sending" class="min-h-12 rounded-xl bg-[color:var(--bg-button)] px-5 py-3 font-semibold text-[color:var(--text-button)] disabled:opacity-60">{{ sending ? 'Enviando…' : 'Enviar mensagem' }}</button></form>
</PublicPage></template>
<script setup lang="ts">definePageMeta({ layout: 'public' }); const form = reactive({ name:'', email:'', subject:'', message:'' }); const { sending, sent, error, submit } = useContactForm()</script>
<style scoped>.field{display:block;width:100%;margin-top:.5rem;border:1px solid var(--border);border-radius:.75rem;background:var(--surface-2);padding:.75rem;color:var(--text)}label{display:block;font-size:.875rem;font-weight:600}</style>
